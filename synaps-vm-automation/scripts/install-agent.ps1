# synaps-vm-agent Windows install placeholder.
# Generate tokens locally, do not print tokens by default, store config/token under protected user/service app-data.
# Default localhost bind should not create an inbound firewall rule. Non-local bind requires explicit firewall warning/rule.
Write-Host "synaps-vm-agent Windows install placeholder"
